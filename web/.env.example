# Public Railway API origin including the /v1 prefix. This is the only browser-side configuration.
NEXT_PUBLIC_API_BASE_URL=https://api.example.up.railway.app/v1
# The API owner enables live demo trials server-side with DEMO_LIVE_TRIALS_ENABLED=true.
